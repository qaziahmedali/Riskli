export interface Step {
  id: number;
  icon: string; // Changed to string for image paths
  title: string;
  description: string;
}

export interface StepCardProps {
  icon: string; // Changed to string for image paths
  title: string;
  description: string;
}
